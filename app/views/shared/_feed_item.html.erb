<li id="<%= feed_item.id %>">
	<%= link_to gravatar_for(feed_item.user), feed_item.user %>
	<p><span><%= link_to feed_item.user.name, feed_item.user %></span> <%= feed_item.content %>
	<p>Posted <%= time_ago_in_words(feed_item.created_at) %> ago.</p>
</li>